class Solution {
public:
    void moveZeroes(vector<int>& nums) {
        int border = -1;
        for(int i=0;i<nums.size();i++){
            if(nums[i] != 0){
                border++;
                swap(nums[border], nums[i]);
            }
        }
    }
};